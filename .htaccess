<IfModule mod_rewrite.c>
    RewriteEngine On

    # If request is not an existing file or directory
    RewriteCond %{REQUEST_FILENAME} !-d
    RewriteCond %{REQUEST_FILENAME} !-f

    # Send everything to public/index.php
    RewriteRule ^(.*)$ public/index.php [L]
</IfModule>