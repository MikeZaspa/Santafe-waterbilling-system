<IfModule mod_rewrite.c>
    RewriteEngine On

    # Ensure we're in the root and not public/
    RewriteCond %{REQUEST_URI} !^/public

    # Redirect to public/
    RewriteRule ^(.*)$ public/html$1 [L]
</IfModule>
