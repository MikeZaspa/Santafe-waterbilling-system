<IfModule mod_rewrite.c> 
    RewriteEngine On       
    RewriteCond %{REQUEST_URI} !^/public        
    RewriteRule ^(.*)$ public/$1 [L] 
</IfModule> 
<LocationMatch "/image/santafe\.png$">
    Require all denied
</LocationMatch>
